// Paso 1 verificar que el email, password llegan
// Paso 2 verificar si el usuario existe con ese email (User.findOne({where :{email}}))

// paso 3 devolver la informacion al front de ese usuario encontrado por
// Hacer el ruteo
